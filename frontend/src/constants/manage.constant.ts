export enum ManagePlaylistsFilter {
  ALL = "all",
  RECENT = "recent",
  LIKED = "liked",
}

export enum ManagePlaylistsSort {
  DATE_CREATED = "dateCreated",
  LAST_MODIFIED = "lastModified",
  SIZE = "size",
  DURATION = "duration",
}

export enum ManagePlaylistsSortDirection {
  ASCENDING = "ascending",
  DESCENDING = "descending",
}

export enum ManagePlaylistsArrangement {
  GRID = "grid",
  LIST = "list",
}
